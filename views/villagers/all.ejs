<%- include('../partials/header', { user }) %>

<div>
    <% villager.forEach(function(villagers) { %>
        <a href="villagers/<%= villagers.id %>"><%= villagers.name['name-USen'] %></a>
        <div>
            <img src="<%= villagers.image_uri %>"/> 
        </div>
    <% }) %>
</div>

<%- include('../partials/footer') %>